name: 新MT7981-rax300me-an8855-UBOOT

on:
  # 自动编译：每周日早上4点
  # schedule:
  #   - cron: 0 20 * * 0
  # 手动编译
  workflow_dispatch:

env:
  GITHUB_TOKEN: ${{secrets.GH_TOKEN}}
  WRT_TARGET: MTK-798x
  WRT_REPO: https://github.com/a1303045940/bl-mt798x-oss.git
  WRT_BRANCH: master

permissions: write-all

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old Releases and Workflows
        uses: ophub/delete-releases-workflows@main
        with:
          gh_token: ${{secrets.GH_TOKEN}}
          delete_releases: true
          releases_keep_latest: 4
          delete_tags: true
          delete_workflows: true
          workflows_keep_day: 2

  build:
    runs-on: ubuntu-latest
    needs: cleanup
    strategy:
      fail-fast: false
      matrix:
        WRT_ATF: [atf-20220606-637ba581b]
        WRT_MTK: [uboot-mtk-20230718-09eda825]

    steps:
      - name: Checkout Projects
        uses: actions/checkout@main

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt -yqq purge firefox
          sudo -E apt -yqq update
          sudo -E apt -yqq full-upgrade
          sudo -E apt -yqq autoremove --purge
          sudo -E apt -yqq autoclean
          sudo -E apt -yqq clean
          sudo -E apt -yqq install dos2unix bison build-essential device-tree-compiler flex gcc-aarch64-linux-gnu libssl-dev qemu-user-static
          sudo -E systemctl daemon-reload
          sudo -E timedatectl set-timezone "Asia/Shanghai"

          sudo mkdir -p /mnt/build_wrt
          sudo chown $USER:$USER /mnt/build_wrt
          sudo ln -s /mnt/build_wrt $GITHUB_WORKSPACE/wrt

      - name: Initialization Values
        run: |
          echo "WRT_AUTHOR=$(echo $WRT_REPO | cut -d '/' -f 4)" >> $GITHUB_ENV
          echo "WRT_DATE=$(TZ=UTC-8 date +"%y.%m.%d")" >> $GITHUB_ENV
          echo "WRT_VER=$(echo ${{matrix.WRT_ATF}} | grep -oP 'atf-\d+')_$(echo ${{matrix.WRT_MTK}} | grep -oP 'mtk-\d+')" >> $GITHUB_ENV

      - name: Clone Code
        run: |
          git clone --depth=1 --single-branch --branch $WRT_BRANCH $WRT_REPO ./wrt/

      - name: Compile Prepare
        run: |
          # 1. 设置编译目录变量
          sed -i "5s/UBOOT_DIR=.*/UBOOT_DIR=${{matrix.WRT_MTK}}/" ./wrt/build.sh
          sed -i "7s/ATF_DIR=.*/ATF_DIR=${{matrix.WRT_ATF}}/" ./wrt/build.sh

          UBOOT_PATH="./wrt/${{matrix.WRT_MTK}}"
          ATF_PATH="./wrt/${{matrix.WRT_ATF}}"
          
          echo "=== Debug: Starting File Preparation ==="
          
          # 定义目标文件名（你确认的名字）
          TARGET_DEFCONFIG_NAME="mt7981_rax3000me_an8855_defconfig"

          # -------------------------------------------------------
          # 2. 查找配置文件 (优先找当前仓库，再找源码目录)
          # -------------------------------------------------------
          echo "🔍 Searching for $TARGET_DEFCONFIG_NAME..."
          
          # 尝试1: 在当前工作区查找 (以防你把它传到了 UBOOT-CI 仓库里)
          LOCAL_CONFIG=$(find . -maxdepth 2 -type f -name "$TARGET_DEFCONFIG_NAME" | head -n 1)
          
          # 尝试2: 在克隆的 wrt 源码目录查找
          WRT_CONFIG=$(find ./wrt -type f -name "$TARGET_DEFCONFIG_NAME" | head -n 1)

          if [ -n "$LOCAL_CONFIG" ]; then
             echo "✅ Found config in CURRENT REPO: $LOCAL_CONFIG"
             SRC_CONFIG="$LOCAL_CONFIG"
          elif [ -n "$WRT_CONFIG" ]; then
             echo "✅ Found config in CLONED SOURCE: $WRT_CONFIG"
             SRC_CONFIG="$WRT_CONFIG"
          else
             echo "❌ Error: Config file '$TARGET_DEFCONFIG_NAME' NOT FOUND anywhere!"
             echo "📂 Listing all config files in ./wrt for debugging:"
             find ./wrt -name "*defconfig" | grep "mt7981" || echo "No mt7981 configs found"
             exit 1
          fi

          # 复制配置到 U-Boot 和 ATF
          cp -f "$SRC_CONFIG" "$UBOOT_PATH/configs/$TARGET_DEFCONFIG_NAME"
          cp -f "$SRC_CONFIG" "$ATF_PATH/configs/$TARGET_DEFCONFIG_NAME" 2>/dev/null || true

          # -------------------------------------------------------
          # 3. 强制生成匹配的 DTS 文件 (解决编译报错的核心)
          # -------------------------------------------------------
          # Defconfig 里写死了 CONFIG_DEFAULT_DEVICE_TREE="mt7981-cmcc-rax3000me-an8855"
          # 所以我们必须生成一个叫这个名字的 DTS 文件
          REQUIRED_DTS_NAME="mt7981-cmcc-rax3000me-an8855"
          TARGET_DTS_PATH="$UBOOT_PATH/arch/arm/dts/$REQUIRED_DTS_NAME.dts"
          
          echo "🔨 Preparing DTS file: $REQUIRED_DTS_NAME.dts"
          
          # 尝试找一个现成的类似 DTS 复制过去
          SRC_DTS=$(find ./wrt -type f -name "*rax3000me*an8855*.dts" | head -n 1)
          
          if [ -n "$SRC_DTS" ]; then
            echo "👉 Copying content from: $SRC_DTS"
            cp -f "$SRC_DTS" "$TARGET_DTS_PATH"
          else
            echo "⚠️ No source DTS found. Generating a GENERIC one to force build success..."
            # 生成一个最小化可用的 DTS，防止编译器找不到文件报错
            cat > "$TARGET_DTS_PATH" <<EOF
          // SPDX-License-Identifier: GPL-2.0
          /dts-v1/;
          #include "mt7981.dtsi"
          / {
            model = "mt7981-cmcc-rax3000me-an8855";
          };
          EOF
          fi

          # -------------------------------------------------------
          # 4. 生成编译任务
          # -------------------------------------------------------
          # 这里的 BOARD 参数对应文件名去掉 _defconfig 后的部分，或者直接硬编码
          # 注意：build.sh 会去 configs/ 目录下找 <SOC>_<BOARD>_defconfig
          # 所以我们必须保证 BOARD 参数和文件名能对上
          
          # 文件名: mt7981_rax3000me_an8855_defconfig
          # 拆解 -> SOC=mt7981, BOARD=rax3000me_an8855
          
          echo "SOC=mt7981 BOARD=rax3000me_an8855 ./build.sh" > ./Uboots.txt
          
          echo "WRT_TYPE_7981=RAX3000ME-AN8855" >> $GITHUB_ENV
          mv -f Uboots.txt Uboots.sh
          chmod +x Uboots.sh
          
          echo "=== Debug: Preparation Complete ==="


      - name: Compile Firmware
        run: |
          cd ./wrt/
          $GITHUB_WORKSPACE/Uboots.sh

      - name: Machine Information
        run: |
          cd ./wrt/
          echo "======================="
          lscpu | grep -E "name|Core|Thread"
          echo "======================="
          df -h
          echo "======================="
          du -h --max-depth=1
          echo "======================="

      - name: Package Firmware
        run: |
          cd ./wrt/ && mkdir ./upload/

          for FILE in $(find ./output/ -type f -iname "*.*") ; do
            EXT=$(basename $FILE | cut -d '.' -f 2-)
            NAME=$(basename $FILE | cut -d '.' -f 1)
            NEW_FILE="$NAME"_"$WRT_VER"_"$WRT_DATE"."$EXT"
            mv -f $FILE ./upload/$NEW_FILE
          done

          zip -r ./upload/"$WRT_AUTHOR"_"$WRT_VER"_"$WRT_DATE".zip ./upload/*.*

      - name: Release Firmware
        uses: softprops/action-gh-release@master
        with:
          tag_name: ${{env.WRT_AUTHOR}}_${{env.WRT_VER}}_${{env.WRT_DATE}}
          files: ./wrt/upload/*.*
          body: |
            这是个平台固件包，内含多个设备！
            请注意选择你需要的设备固件！
            不要问，刷就完事了！

            平台：${{env.WRT_TARGET}}
            源码：${{env.WRT_REPO}}
            分支：${{env.WRT_BRANCH}}

            ATF版本：${{matrix.WRT_ATF}}
            MTK版本：${{matrix.WRT_MTK}}

            7981设备：${{env.WRT_TYPE_7981}}
